<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mango.focustime">

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".FocusModeActivity"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PunishmentActivity" />
        <activity
            android:name=".ToDoActivity"
            android:label="Todo List"
            android:parentActivityName=".FocusModeActivity" />
        <activity android:name=".EditorActivity" />
        <activity
            android:name=".HowToUseActivity"
            android:label="How to use?"
            android:parentActivityName=".FocusModeActivity" />

        <provider
            android:name=".data.TodoProvider"
            android:authorities="com.example.mango.focustime"
            android:exported="false" />

        <application
            android:name=".MyApplication"
            android:icon="@drawable/icon"
            android:label="@string/app_name" />

        <service
            android:name=".DetectionService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/detection_service_config" />
        </service>
        <service android:name=".service.MyService" />

        <receiver android:name=".receiver.MyReceiver" />

        <activity android:name=".SettingsActivity"
            android:label="Settings"
            android:parentActivityName=".FocusModeActivity"></activity>
    </application>

</manifest>