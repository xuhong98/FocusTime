<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mango.focustime">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ToDoActivity"
            android:label="Todo List"
            android:parentActivityName="com.example.mango.focustime.MainActivity">
        </activity>
        <activity android:name=".FocusModeActivity"
            android:parentActivityName="com.example.mango.focustime.MainActivity"/>
        <activity android:name=".PunishmentActivity"></activity>
        <activity android:name=".EditorActivity"></activity>

        <provider
            android:name=".data.TodoProvider"
            android:authorities="com.example.mango.focustime"
            android:exported="false" />

        <application
            android:name="com.example.mango.focustime.MyApplication"
            android:icon="@drawable/icon"
            android:label="@string/app_name" />

        <service
            android:name="com.example.mango.focustime.DetectionService"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">

            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService"/>
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/detection_service_config"/>
        </service>

        <service android:name="com.example.mango.focustime.service.MyService" />

        <receiver android:name="com.example.mango.focustime.receiver.MyReceiver" />


    </application>

    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />

</manifest>